<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Created_By__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <description>Indicates the user that initiated the notification. This is created as a custom field instead of using the standard created by because these logs are created via platform event (asynchronously) and always holds the process automation user name. Its not a formula field as PE Logger is a text field and not a lookup</description>
    <label>Created By</label>
    <referenceTo>User</referenceTo>
    <relationshipName>Communication_Logs</relationshipName>
    <required>false</required>
    <trackTrending>false</trackTrending>
    <type>Lookup</type>
</CustomField>
